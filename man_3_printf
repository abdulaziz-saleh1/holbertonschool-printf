.TH _PRINTF 3 "March 2025" "Holberton School" "Custom Printf Project"

.SH NAME
_printf \- Custom implementation of the standard C printf function.

.SH SYNOPSIS
.B #include "main.h"

.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf
function is a simplified version of the standard C library printf function.
It writes output to the standard output stream (stdout), based on a format string and a variable number of arguments.

Supported conversion specifiers:

- %c: Prints a single character  
- %s: Prints a string  
- %%: Prints the percent symbol  
- %d: Prints a decimal integer  
- %i: Prints an integer (same as %d)

.SH RETURN VALUE
Returns the number of characters printed (excluding the null byte).
Returns -1 if an error occurs (e.g., invalid format string).

.SH EXAMPLES
_printf("Hello %s!\n", "world");  
=> Prints: Hello world!

_printf("Value: %d\n", 42);  
=> Prints: Value: 42

.SH AUTHOR
Written by Holberton School students.

.SH SEE ALSO
printf(3), write(2), stdarg(3)
